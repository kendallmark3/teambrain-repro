# Base URL for the backend API.
# In development this is omitted — Vite's proxy rewrites /api → http://localhost:3001.
# In production set this to the absolute API origin, e.g.:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
